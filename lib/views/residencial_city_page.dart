import 'package:flutter/material.dart';

class ResidencialCityPage extends StatelessWidget {
  const ResidencialCityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Residencial City'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text(''), // Empty body
      ),
    );
  }
}
