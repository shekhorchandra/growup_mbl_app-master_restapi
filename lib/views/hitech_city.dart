import 'package:flutter/material.dart';

class HitechCityPage extends StatelessWidget {
  const HitechCityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Hi-Tech City'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text(''), // Empty body
      ),
    );
  }
}